# FastAPI backend dependencies for separator-env (pipeline API, Phase 0+)
fastapi
uvicorn
anyio
httpx
pytest
pytest-asyncio
# P2 optional: local CPU phoneme alignment (downloads MMS model on first use)
ctc-forced-aligner>=1.0.2
