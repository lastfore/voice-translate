# 阶段关键日志增强 — 测试执行记录

> **对应文档：** [阶段关键日志增强方案.md](./阶段关键日志增强方案.md)、[阶段关键日志增强测试文档.md](./阶段关键日志增强测试文档.md)  
> **执行日期：** 2026-07-29  
> **执行命令：** `pytest tests/test_stage_log.py -q`、`pytest tests/test_stage_log_integration.py -q`、`pytest tests/test_runner.py -q`、`pytest tests/api/test_stage_sse.py -q`、`pytest tests -q`

---

## Phase 1：基础设施

| 用例编号 | 测试结果 | 缺陷单号 | 备注 |
|---------|---------|---------|------|
| P1-TC-001 | 通过 | 无 | LRC slice 分组不含 VAD 参数 |
| P1-TC-002 | 通过 | 无 | advanced/defaulted 差异识别正确 |
| P1-TC-003 | 通过 | 无 | [JOB]→[INPUT]→[PARAM] 顺序稳定 |
| P1-TC-004 | 通过 | 无 | [CMD] 三行 python/cwd/argv 完整 |

**Phase 1 结论：** `tests/test_stage_log.py` 6/6 通过。

---

## Phase 2：Runner 层

| 用例编号 | 测试结果 | 缺陷单号 | 备注 |
|---------|---------|---------|------|
| P2-TC-001 | 通过 | 无 | 入队前 log 已含 [JOB]/[INPUT]/[PARAM] |
| P2-TC-002 | 通过 | 无 | started_at、queue_wait_ms 非负整数 |
| P2-TC-003 | 通过 | 无 | 异常路径含 [ERR] 与 success=false |
| P2-TC-004 | 通过 | 无 | 取消路径 footer 完整 |

**Phase 2 结论：** `tests/test_stage_log_integration.py` Runner 相关 4/4 通过。

---

## Phase 3：Subprocess 阶段（separate / convert）

| 用例编号 | 测试结果 | 缺陷单号 | 备注 |
|---------|---------|---------|------|
| P3-TC-001 | 通过 | 无 | [CMD]/[PROC]/[OUT] 链路完整 |
| P3-TC-002 | 通过 | 无 | slice_batch 输入/参数/输出契约正确 |
| P3-TC-003 | 通过 | 无 | full_track output_path 与 full_track 输出 |
| P3-TC-004 | 通过 | 无 | stage_log=None 签名兼容 |

**Phase 3 结论：** 4/4 通过。

---

## Phase 4：In-process 阶段（slice / merge）

| 用例编号 | 测试结果 | 缺陷单号 | 备注 |
|---------|---------|---------|------|
| P4-TC-001 | 通过 | 无 | LRC [EXEC] handler 上下文 |
| P4-TC-002 | 通过 | 无 | VAD [EXEC] 含 vad 参数 |
| P4-TC-003 | 通过 | 无 | merge [EXEC]+[PROC]+[OUT] |
| P4-TC-004 | 通过 | 无 | inputs/params 仅 Runner 调用一次 |

**Phase 4 结论：** 4/4 通过。

---

## Phase 5：回归验收

| 用例编号 | 测试结果 | 缺陷单号 | 备注 |
|---------|---------|---------|------|
| P5-TC-001 | 通过 | 无 | SSE log_line 字段仍可用（test_stage_sse.py） |
| P5-TC-002 | 通过 | 无 | 取消流程兼容（test_cancellation.py） |
| P5-TC-003 | 通过 | 无 | 全量 API/单元测试无行为回归 |

**Phase 5 结论：** 回归测试通过；mock 签名已补充 `stage_log` 参数。

---

## 整体验收（ALL-TC）

| 用例编号 | 测试结果 | 缺陷单号 | 备注 |
|---------|---------|---------|------|
| ALL-TC-001 | 通过 | 无 | 四阶段 pipeline 各 job log 含头/尾/输出 |
| ALL-TC-002 | 部分自动化 | 无 | slice lrc/vad、convert 模式由集成 mock 覆盖；full_track 单独用例覆盖 |
| ALL-TC-003 | 手动 | — | v1 不改前端协议，SSE log_line 回归已通过 |
| ALL-TC-004 | 部分自动化 | 无 | 失败/取消由 P2-TC-003/004 覆盖 |
| ALL-TC-005 | 通过 | 无 | 前缀受控、路径为 repo 相对路径 |
| ALL-TC-006 | 未测 | — | 性能基线留待手动对比 |

---

## 全量测试汇总

```
pytest tests -q
190 passed, 5 skipped in ~9s
```

**交付判定：** 各 Phase 通过准则满足；自动化用例全部通过；可进入联调/手动 UI 验收（ALL-TC-003/006）。
